apiVersion: s3tables.services.k8s.aws/v1alpha1
kind: Table
metadata:
  name: $TABLE_ADOPTION_CR_NAME
  annotations:
    services.k8s.aws/adoption-policy: adopt
    services.k8s.aws/adoption-fields: "$ADOPTION_FIELDS"
    services.k8s.aws/deletion-policy: retain
